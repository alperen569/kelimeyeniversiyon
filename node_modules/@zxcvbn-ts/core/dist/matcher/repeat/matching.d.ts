import { MatcherBaseClass, MatchOptions, RepeatMatch } from '../../types';
import Matching from '../../Matching';
import Options from '../../Options';
import { MaybePromise } from 'rollup';
declare class MatchRepeat extends MatcherBaseClass {
    private scoring;
    constructor(options: Options);
    match({ password, omniMatch }: MatchOptions): RepeatMatch[] | Promise<RepeatMatch[]>;
    normalizeMatch(baseToken: string, j: number, match: RegExpExecArray, baseGuesses: number | Promise<number>): MaybePromise<RepeatMatch>;
    getGreedyMatch(password: string, lastIndex: number): RegExpExecArray | null;
    getLazyMatch(password: string, lastIndex: number): RegExpExecArray | null;
    setMatchToken(greedyMatch: RegExpExecArray, lazyMatch: RegExpExecArray | null): {
        match: RegExpExecArray | null;
        baseToken: string;
    };
    getBaseGuesses(baseToken: string, omniMatch: Matching): number | Promise<number>;
}
export default MatchRepeat;
