import { MatcherBaseClass, DateMatch, MatchOptions } from '../../types';
type DateMatchOptions = Pick<MatchOptions, 'password'>;
declare class MatchDate extends MatcherBaseClass {
    match({ password }: DateMatchOptions): import("../..").MatchExtended[];
    getMatchesWithSeparator(password: string): DateMatch[];
    getMatchesWithoutSeparator(password: string): DateMatch[];
    filterNoise(matches: DateMatch[]): DateMatch[];
    mapIntegersToDayMonthYear(integers: number[]): {
        year: number;
        month: number;
        day: number;
    } | null;
    getDayMonth(integers: number[]): {
        year: number;
        month: number;
        day: number;
    } | null;
    mapIntegersToDayMonth(integers: number[]): {
        day: number;
        month: number;
    } | null;
    twoToFourDigitYear(year: number): number;
}
export default MatchDate;
