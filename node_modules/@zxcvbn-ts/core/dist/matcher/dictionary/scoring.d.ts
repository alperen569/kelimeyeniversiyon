import { DictionaryScoringExtras, MatchEstimated, MatchExtended } from '../../types';
export interface DictionaryReturn extends DictionaryScoringExtras {
    calculation: number;
}
declare const _default: ({ rank, reversed, l33t, subs, token, dictionaryName, }: MatchExtended | MatchEstimated) => DictionaryReturn;
export default _default;
