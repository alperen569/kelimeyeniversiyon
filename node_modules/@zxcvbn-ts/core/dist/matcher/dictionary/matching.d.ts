import Options from '../../Options';
import { DictionaryMatch, MatcherBaseClass } from '../../types';
import { DictionaryMatchOptions } from './types';
declare class MatchDictionary extends MatcherBaseClass {
    protected wordSequenceCheck?: boolean | undefined;
    constructor(options: Options, wordSequenceCheck?: boolean | undefined);
    private getRangedDictionaries;
    match({ password, userInputsOptions, useLevenshtein, }: DictionaryMatchOptions): DictionaryMatch[];
}
export default MatchDictionary;
