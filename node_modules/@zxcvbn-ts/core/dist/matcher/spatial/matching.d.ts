import { MatcherBaseClass, MatchOptions, OptionsGraphEntry, SpatialMatch } from '../../types';
type SpatialMatchOptions = Pick<MatchOptions, 'password'>;
declare class MatchSpatial extends MatcherBaseClass {
    SHIFTED_RX: RegExp;
    match({ password }: SpatialMatchOptions): import("../..").MatchExtended[];
    checkIfShifted(graphName: string, password: string, index: number): 0 | 1;
    helper(password: string, graph: OptionsGraphEntry, graphName: string): SpatialMatch[];
}
export default MatchSpatial;
