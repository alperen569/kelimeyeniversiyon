import { MatcherBaseClass, MatchOptions, SequenceMatch } from '../../types';
interface UpdateParams {
    i: number;
    j: number;
    delta: number;
    password: string;
    result: any[];
}
type SequenceMatchOptions = Pick<MatchOptions, 'password'>;
declare class MatchSequence extends MatcherBaseClass {
    MAX_DELTA: number;
    match({ password }: SequenceMatchOptions): SequenceMatch[];
    update({ i, j, delta, password, result }: UpdateParams): number | null;
    getSequence(token: string): {
        sequenceName: string;
        sequenceSpace: number;
    };
}
export default MatchSequence;
